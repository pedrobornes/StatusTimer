# Railway / Nixpacks — Spring Boot backend (Java 21 + Maven)
[variables]
NIXPACKS_JDK_VERSION = "21"

[phases.build]
cmds = ["mvn -DskipTests package"]

[start]
cmd = "java -jar target/statustimer-api-0.0.1-SNAPSHOT.jar"
