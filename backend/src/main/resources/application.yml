server:
  port: 8080

spring:
  application:
    name: statustimer-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  jpa:
    open-in-view: false

app:
  security:
    api-key: ${APP_API_KEY:your-local-secret-key}
